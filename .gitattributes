# Line endings are LF everywhere, in the repository and in the working tree.
# Git checks nothing out as CRLF, so the same bytes are on every machine and a
# checkout on Windows reads the same as one on macOS or Linux.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
